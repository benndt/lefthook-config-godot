min_version: 2.0.0

pre-commit:
  parallel: false
  jobs:
    - name: linter
      group:
        parallel: true
        jobs:
          - name: gdlint
            glob: "*.gd"
            run: gdlint {staged_files}